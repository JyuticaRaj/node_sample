
const http=require('http');
const url=require('url');
const port=3000;
const users=[{
    name:'Ammu',
    mark:55
},{
    name:'bibin',
    mark:56
}
]

const server=http.createServer(function(req,res){


if (URL.includes('getuser'){
    resizeBy.end(JASON.stringify(users));
    {
        elseif(URL.includes('insertUser'))
        {
            let 
        }
    }
})